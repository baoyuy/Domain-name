{
  "name": "oneproxy-cli",
  "version": "1.0.0",
  "description": "One-command interactive domain reverse proxy manager",
  "bin": {
    "oneproxy": "./src/cli.js"
  },
  "scripts": {
    "start": "node ./src/cli.js",
    "check": "node --check ./src/cli.js && node --check ./src/lib/app.js && node --check ./src/lib/caddy.js && node --check ./src/lib/store.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
